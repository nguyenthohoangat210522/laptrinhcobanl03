#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int x,a,b,c;
    scanf("%d",&x);
    a=pow(x,2);
    b=pow(x,3);
    c=pow(x,4);
    printf("%d\n%d\n%d",a,b,c);

    return 0;
}
